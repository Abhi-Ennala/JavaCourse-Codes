public class Strings {
    public static void main(String args[]){
        String name = new String("Abhinav");
        String Name = "Abhinav";
        System.out.println(name);
        System.out.println(Name);
    // Here only one object of string class is created. When two variables( obj) have same value, only one is allocated space and memory and both variables(obj) points to it
    }
}
